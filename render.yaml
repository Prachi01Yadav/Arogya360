services:
  - type: web
    name: arogya360
    env: docker
    plan: free
    branch: main
    envVars:
      - key: DB_URL
        value: jdbc:mysql://your-mysql-host:3306/hospital_db
      - key: DB_USERNAME
        value: root
      - key: DB_PASSWORD
        value: your-password
